<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net472</TargetFramework>
    <AssemblyName>ERC.Net</AssemblyName>
    <RootNamespace>ERC</RootNamespace>
    <GenerateDocumentationFile>true</GenerateDocumentationFile>
    <!--
      The XML file feeds the DocFX site, but a few dozen public members predate
      the doc comments and drown out real warnings. Filling these in is tracked as
      part of the documentation pass; suppressed here to keep build output useful.
    -->
    <NoWarn>$(NoWarn);CS1591;CS1573</NoWarn>
  </PropertyGroup>

  <ItemGroup>
    <!--
      Reloaded.Assembler brings its own FASM native binaries, so the assembler and
      disassembler need no hand-placed DLLs. The previous HintPath references
      pointed at a "ConsoleApp1" folder on the original author's machine and never
      resolved on any other checkout.
    -->
    <PackageReference Include="Reloaded.Assembler" Version="1.0.14" />
    <PackageReference Include="Reloaded.Memory" Version="8.0.1" />
    <PackageReference Include="SharpDisasm" Version="1.1.11" />
  </ItemGroup>

  <ItemGroup>
    <!--
      Reloaded's dependencies resolve to older versions of these three than the
      ones it actually binds against. Pinning them here unifies the graph for
      everything downstream; without it the build reports unresolvable conflicts
      and the merged plugin can fail to load at run time.
    -->
    <PackageReference Include="System.Memory" Version="4.5.5" />
    <PackageReference Include="System.Numerics.Vectors" Version="4.5.0" />
    <PackageReference Include="System.Runtime.CompilerServices.Unsafe" Version="6.0.0" />
  </ItemGroup>

</Project>
