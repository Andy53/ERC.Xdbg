<Project>

  <!--
    Shared build configuration for every project in the repository.

    Architecture note: x64dbg loads plugins as native DLLs, so the plugin and its
    dependencies must stay on .NET Framework and must be built for a concrete
    architecture. "AnyCPU" is deliberately not offered - a plugin built AnyCPU
    cannot carry the native export table x64dbg requires.
  -->

  <PropertyGroup>
    <Platforms>x86;x64</Platforms>
    <Platform Condition="'$(Platform)' == '' Or '$(Platform)' == 'AnyCPU'">x86</Platform>
    <PlatformTarget>$(Platform)</PlatformTarget>

    <LangVersion>latest</LangVersion>

    <!--
      Nullable reference types on, everywhere. This is part of the public contract:
      consumers of the NuGet package see which arguments and results can be null.
    -->
    <Nullable>enable</Nullable>

    <!--
      Every warning is an error. The build is at zero warnings across all four legs
      (x86 and x64, net472 and net8.0-windows) and the point of getting there is that
      it stays there - a warning nobody acts on is worse than not having enabled it.

      Where a warning is genuinely not worth acting on, it is suppressed explicitly
      and with a reason: see the NoWarn entries in ERC.Net.csproj for the deliberate
      obsoletions, and the file-scoped pragmas in Native/ for the P/Invoke
      declarations that Microsoft already documents.
    -->
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <!--
      Off. The only unsafe code was ModuleInfo's PE header parsing, which cast a
      pointer over a fixed buffer and followed an offset taken from the file with no
      bounds check. That is now ERC.Utilities.PeHeaders, where every read is checked
      by the runtime. Leaving the switch off means the next such block does not
      compile silently.
    -->
    <AllowUnsafeBlocks>false</AllowUnsafeBlocks>

    <!--
      Deterministic compilation: the same source produces the same IL, with a
      content-derived module version id rather than a random one, and no machine
      paths baked in.

      This does not make the shipped plugin byte-for-byte reproducible, and it is
      worth being clear about why. The compiler's output is rewritten twice after
      it is produced - once by Fody/Costura to embed the managed dependencies, and
      once by the export weaver to add the native entry points x64dbg calls - and
      each re-emits the PE file with a fresh timestamp and module version id.
      Two builds of identical source differ in exactly those 18 bytes.

      That is why every release publishes a SHA-256 of the archive rather than
      inviting users to rebuild and compare.
    -->
    <Deterministic>true</Deterministic>
    <ContinuousIntegrationBuild Condition="'$(CI)' == 'true'">true</ContinuousIntegrationBuild>

    <!-- Restore must never silently reach for a package that was not asked for. -->
    <RestoreLockedMode Condition="'$(CI)' == 'true'">true</RestoreLockedMode>

    <Company>ERC</Company>
    <Authors>Andy Bowden</Authors>
    <Copyright>Copyright (c) Andy Bowden</Copyright>
    <Product>Erc.Xdbg</Product>
    <Version>3.0.0</Version>

    <!--
      x64dbg reads the plugin's architecture from the file extension, and the
      whole toolchain keys off this one property rather than hand-edited source.
    -->
    <ErcArchSuffix Condition="'$(Platform)' == 'x64'">64</ErcArchSuffix>
    <ErcArchSuffix Condition="'$(Platform)' == 'x86'">32</ErcArchSuffix>
  </PropertyGroup>

  <PropertyGroup>
    <!--
      X64 is the single switch that selects 64-bit interop across the solution.
      It replaces six hand-edited "uncomment for 64bit" sites.
    -->
    <DefineConstants Condition="'$(Platform)' == 'x64'">$(DefineConstants);X64</DefineConstants>
  </PropertyGroup>

  <PropertyGroup>
    <!-- Costura embeds dependencies; its IncludeAssets advice does not apply here. -->
    <NoWarn>$(NoWarn);FodyPackageReference</NoWarn>
  </PropertyGroup>

</Project>
